# Screenshots

Add screenshots here for the main README:

- `dashboard.png` — the `/dashboard` page, logged in as the demo account
- `insights.png` — the `/insights` page, showing the forecast chart and ML insights

Quick way to generate them:

```bash
docker compose up --build
# open http://localhost:5173, log in as john@example.com / password123
# screenshot /dashboard and /insights, save here as dashboard.png / insights.png
```
